allowBuilds:
  esbuild: true
overrides:
  # Force the patched esbuild (tsup@8.5.1 pins ^0.27.0, which is < the 0.28.1
  # fix for GHSA dev-only RCE/file-read advisories).
  esbuild: "^0.28.1"
minimumReleaseAgeExclude:
  - "@ai-sdk/gateway@3.0.132"
  - "@ai-sdk/react@3.0.208"
  - ai@6.0.206
